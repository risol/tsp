[package]
name = "tsp-http"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Small HTTP/1.1 parser and response writer for TSP"

[dependencies]
tsp-core = { path = "../tsp-core" }
