[package]
name = "tsp-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Stable TSP domain and host/worker protocol types"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
